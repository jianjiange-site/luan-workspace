<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dating.post.mapper.PostLikeMapper">

    <!--
      Idempotent like upsert.
      INSERT on first like → ON CONFLICT (user_id, post_id) → UPDATE status.
      WHERE clause skips update when status is already the target (idempotent).
      Returns 0 rows affected in that case, 1 when state actually changed.
    -->
    <insert id="upsert">
        INSERT INTO post_likes (user_id, post_id, status, created_at, updated_at)
        VALUES (#{userId}, #{postId}, #{status}, NOW(), NOW())
        ON CONFLICT (user_id, post_id)
        DO UPDATE SET status = EXCLUDED.status, updated_at = NOW()
        WHERE post_likes.status &lt;&gt; EXCLUDED.status
    </insert>

</mapper>
